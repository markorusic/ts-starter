const main = () => {
  document.querySelector('#app').innerHTML = `
    <h1>ts-starter</h1>
    <p>Change your src/app.ts</p>
  `
}

window.addEventListener('DOMContentLoaded', main)

export default main
